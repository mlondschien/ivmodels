name: ivmodels
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python>=3.7
  - numpy
  - scipy
  - pytest  # tests only
  - pytest-cov  # tests only
  - linearmodels  # tests only
  - scikit-learn
  - glum
  - numba
  - pandas  # optional